name: Security Audit

# Déclenchement TOUS les lundis à 9h00 UTC
# et sur chaque push vers le main, feat/*, fix/* - et merge sur main

on:
  schedule:
    - cron: "0 9 * * 1"
  push:
    branches: [main, feat/*, fix/*]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  # Audit de sécurité des dépendances
  security-audit:
    name: Security Vulnerability Scan
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20.x"

      # Audit du backend
      - name: Backend Security Audit
        working-directory: ./back
        run: |
          npm audit --audit-level=moderate
          echo "✅ Backend security audit completed"

      # Audit du frontend
      - name: Frontend Security Audit
        working-directory: ./front
        run: |
          npm audit --audit-level=moderate
          echo "✅ Frontend security audit completed"

      # Vérifications supplémentaires de sécurité
      - name: Check for sensitive files
        run: |
          echo "🔍 Checking for sensitive files..."
          # Vérifier qu'il n'y a pas de fichiers sensibles commités
          if find . -name "*.env*" -not -path "./node_modules/*" | grep -v ".env.example" | head -1; then
            echo "WARNING: .env files detected in repository"
            exit 1
          fi
          echo "No sensitive files detected"

      # Résumé final
      - name: Security Summary
        run: |
          echo "Security audit completed successfully!"
          echo "Backend dependencies checked"
          echo "Frontend dependencies checked"
          echo "Sensitive files check passed"
          echo "Repository security validated"
